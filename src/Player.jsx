export function Player({ name, position, overall, pace, shooting, passing, dribbling, defending, physic }){
    return (
        <div>
            {name}
            {position}
            {overall}
        </div>
    )
}